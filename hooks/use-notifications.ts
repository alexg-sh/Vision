import { useNotifications as useNotificationsContext } from '@/context/notification-context';

// Re-export the hook from the context file for conventional use
export const useNotifications = useNotificationsContext;
